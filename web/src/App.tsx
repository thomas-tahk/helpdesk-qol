import { Button } from "@/components/ui/button"

function App() {
  return (
    <main className="min-h-svh flex flex-col items-center justify-center gap-6 p-8">
      <h1 className="text-3xl font-semibold">helpdesk-qol</h1>
      <p className="text-muted-foreground">scaffold check — Tailwind + shadcn working</p>
      <Button>OK</Button>
    </main>
  )
}

export default App
